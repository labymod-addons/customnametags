@import super();

.input-container {
  border-radius: 10;
  background-color: rgba(50, 50, 50, 100);
  filter: blur(5);

  .custom-preview {
    top: 0;
    border-radius: 5;
    padding: 2 4 2 3;
  }

  .label-name {
    margin-left: 23;
  }
}

.remove-container {
  border-radius: 10;
  background-color: rgba(50, 50, 50, 100);
  filter: blur(5);
}